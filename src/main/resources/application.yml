server:
  port: 9090
spring:
  security:
    oauth2:
      client:
        registration:
          #google...
          keycloak:
            #credenciais do cliente oauth2 cadastrado no keycloak
            client-id: spring-security-keycloak
            client-secret: eK0jsvSGkI5KeHZMUeyPp9OD5BSo9oNr
            scope: openid, profile, email
        provider:
          #cliente oauth apontando para o keycloak
          keycloak:
            issuer-uri: http://localhost:8080/realms/luizakuze
      resourceserver:
        jwt:
          #issuer-uri: https://accounts.google.com
          issuer-uri: http://localhost:8080/realms/luizakuze